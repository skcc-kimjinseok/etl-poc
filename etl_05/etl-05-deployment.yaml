apiVersion: apps/v1
kind: Deployment
metadata:
  name: etl-05-pipeline
  namespace: prefect

spec:
  replicas: 1
  selector:
    matchLabels:
      flow: etl-05-pipeline
  template:
    metadata:
      labels:
        flow: etl-05-pipeline
    spec:
      containers:
        - name: etl-05-container
          image: skccetlpipelinedevacr.azurecr.io/etl-05-pipeline:latest
          env:
            - name: PREFECT_API_URL
              value: http://prefect-server/api  